{
  "name": "@products-api/infrastructure",
  "version": "1.0.0",
  "private": true,
  "description": "AWS CDK infrastructure for Products API",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "build:watch": "tsc --project tsconfig.json --watch",
    "synth": "npm run build && cdk synth",
    "deploy": "npm run build && cdk deploy --all --require-approval never",
    "deploy:dev": "npm run build && cdk deploy --all --require-approval never --context env=dev",
    "deploy:prod": "npm run build && cdk deploy --all --require-approval never --context env=prod",
    "destroy": "cdk destroy --all --force",
    "diff": "npm run build && cdk diff",
    "lint": "eslint lib/**/*.ts bin/**/*.ts",
    "clean": "rm -rf dist cdk.out"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.173.0",
    "constructs": "^10.4.2",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "aws-cdk": "^2.173.0",
    "@types/node": "^22.10.0",
    "@types/source-map-support": "^0.5.10",
    "typescript": "^5.7.2",
    "ts-node": "^10.9.2"
  }
}
